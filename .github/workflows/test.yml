name: Jest

on:
  pull_request:
    branches: [main]
  push:
    branches: [test]

jobs:
  jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      

      - name: Jest
        run: yarn test
